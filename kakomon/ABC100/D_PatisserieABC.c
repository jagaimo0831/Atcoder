#include <stdio.h>
int main (void) {
    // input
    int N, M, i;
    double x[1001], y[1001], z[1001];
    scanf("%d %d", &N, &N, &M);
    for(i=0; i<N; i++){
        scanf("%lf %lf %lf", &x[i], &y[i], &z[i]);
    }

    // processing & output
    

}