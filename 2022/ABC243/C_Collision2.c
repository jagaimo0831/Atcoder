#include <stdio.h>
int main (void) {
    // input
    int N;
    scanf("%d",&N);
    int X[N], Y[N];
    for(i=0; i<N; i++) scanf("%d %d", &X[i], &Y[i]);
    cahr str[N];
    scanf("%s", str);

    // processing & output
    
}